[project]
name = "attendance-backend"
version = "0.1.0"
description = "Student Attendance Management System Backend"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.0.0",
    "flask-cors>=4.0.0",
    "werkzeug>=3.0.0",
]
